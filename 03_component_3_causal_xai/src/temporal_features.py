"""Temporal feature engineering (rolling windows, differentials, trends)."""
import pandas as pd

def compute_temporal_features(df: pd.DataFrame, windows: list) -> pd.DataFrame:
    """Computes temporal features for specified window sizes."""
    return df
