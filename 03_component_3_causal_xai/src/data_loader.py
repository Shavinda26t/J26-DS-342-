"""Module for loading Backblaze Q4 2025 raw and processed dataset files."""
import pandas as pd
import os

def load_raw_data(data_dir: str) -> pd.DataFrame:
    """Loads raw Backblaze CSV files from specified directory."""
    return pd.DataFrame()
